# Ne jamais réécrire l'hôte. Un force-www LWS, une fois www en CNAME
# de l'apex, 301 https://www.pilotcore.fr vers lui-même (TooManyRedirects).
# HTTP→HTTPS et le proxy Flask sont le rôle du vhost (apache/pilotcore.conf).
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>
